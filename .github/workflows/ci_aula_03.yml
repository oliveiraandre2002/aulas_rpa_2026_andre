name: CI - Aula 03 (Funções e Estruturas de Dados)

on:
  push:
    branches: [ "master" ]
    paths:
      - "AULA03/**"
      - "tests/test_aula03.py"
  pull_request:
    types: [ opened, synchronize, reopened ]
    branches: [ "master" ]
    paths:
      - "AULA03/**"
      - "tests/test_aula03.py"

jobs:
  validar_aula03:
    name: Lint e Testes – AULA03
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: [ "3.10", "3.11" ]

    steps:
      - name: Checkout do Repositório
        uses: actions/checkout@v4

      - name: Configurar Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ matrix.python-version }}-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-${{ matrix.python-version }}-

      - name: Instalar dependências
        run: |
          python -m pip install --upgrade pip
          pip install flake8 pytest
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Validar Sintaxe (flake8) – AULA03
        run: |
          flake8 AULA03/ --count --select=E9,F63,F7,F82 --show-source --statistics

      - name: Avaliar PEP8 – AULA03
        run: |
          flake8 AULA03/ --count --max-complexity=10 --max-line-length=127 --statistics

      - name: Executar Testes – mod_rh.py
        run: |
          pytest tests/test_aula03.py -v
